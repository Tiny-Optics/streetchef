# Backend API URL (leave empty to use Vite proxy to http://localhost:4000)
VITE_API_URL=

# Vite dev proxy target when VITE_API_URL is empty
VITE_API_PROXY_TARGET=http://localhost:4000

# Optional: Gemini AI (not required for core app)
GEMINI_API_KEY=
